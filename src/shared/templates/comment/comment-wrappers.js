/**
 * 评论区注入的包装元素模板
 *
 * 这些 HTML 会插进 B 站的评论区 DOM：B 站改版时可在服务器上改 hot-config/templates.js，不必发版。
 */
export const commentWrapperTemplates = {
    locationWrapper: '<div id="location" style="margin-left:5px"></div>'
}
